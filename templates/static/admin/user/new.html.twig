{% extends 'admin_layout.html.twig' %}

{% block title %}{{ 'admin_users_index.title' | trans }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12 mt-3 d-flex justify-content-between align-items-center">
                <h1>{{ 'admin_users_new.title' | trans }}</h1>
                <a href="#" class="btn btn-dark text-white">{{ 'admin_users_new.back_index' | trans }}</a>
            </div>
            <div class="col-12">
                <hr>
            </div>
        </div>

        {# todo form à ajouter ici #}
    </div>
{% endblock %}
